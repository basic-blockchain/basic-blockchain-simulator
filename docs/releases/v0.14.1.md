# Release v0.14.1

## Summary

Hotfix release addressing auth token timing and persistence layer consistency
issues from v0.14.0.

## Included Changes

- **Auth Timing.** Fixed JWT token availability check to ensure token is
  persisted before client-side `/auth/me` calls.
- **Persistence Stability.** Verified PostgreSQL store operations maintain
  consistency across user, wallet, and exchange rate repositories.
- **CI Validation.** All tests pass including integration tests with PostgreSQL.

## Validation

- Complete test suite passed:
  - `ci / detect-language`: ✓ SUCCESS
  - `ci / ci`: ✓ SUCCESS (full test suite + pytest)
  - `sast-python`: ✓ SUCCESS
- PostgreSQL integration layer verified stable.

## Migration / breaking notes

- No breaking changes. Fully compatible with v0.14.0.
- No new permissions or schema changes.

## Notes

- Hotfix release recommended for all v0.14.0 deployments.
- Pairs with the frontend v0.8.1 auth token fix.
