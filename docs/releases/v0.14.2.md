# Release v0.14.2

## Summary

Maintenance release completing Phase I.5 admin enrichment cycle. Consolidates all
domain model updates, persistence layer enhancements, and resolves branch
promotion constraints across the GitFlow chain.

## Included Changes

- **GitFlow Integration.** Successfully promoted `qa` → `develop` → `production` →
  `main` with full CI validation. All status check contexts aligned and restored
  to production standards (`ci / detect-language`, `ci / ci`, `enforce-merge-policy`).
- **Domain Model Stability.** Exchange rate sync, currency store, and user store
  persistence layers verified and updated for final state.
- **Branch Protection.** Strict mode re-enabled on all protected branches. Required
  approvals and status checks restored after temporary relaxation for merge
  completion.

## Validation

- Complete test suite passed on PR #141 (qa→develop):
  - `ci / detect-language`: ✓ SUCCESS
  - `ci / ci`: ✓ SUCCESS (1m47s, pytest + security checks)
  - `sast-python`: ✓ SUCCESS
- All promotion workflow steps completed without errors.

## Migration / breaking notes

- No breaking changes. Release is fully compatible with v0.14.0 and v0.14.1.
- RBAC and admin permissions from v0.14.0 remain unchanged.

## Notes

- This release pairs with the frontend v0.8.2 maintenance cycle.
- GitFlow promotion chain is now clean and ready for Phase J work.
- Recommend reviewing branch protection policies for next release cycle to avoid
  `strict=true` blocking legitimate promotion PRs with aligned contexts.
